export default function TemplatesPageHeader() {
  return (
    <header data-templates-page-header>
      <h1 className="text-2xl font-semibold tracking-tight text-slate-900">
        Proposal templates
      </h1>
      <p className="mt-1.5 max-w-2xl text-sm leading-relaxed text-slate-600">
        Pick a template, confirm it is ready, then create proposals from a Job Card. Edit a template
        only when you need to change packages, Catalog links, display, or content.
      </p>
    </header>
  );
}
