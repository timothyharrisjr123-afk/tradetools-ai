import ProposalNoLogoCoverReviewHarness from "./ProposalNoLogoCoverReviewHarness";

/**
 * Presentation harness for true no-logo Preview cover fallback.
 * Not production company data — visual component proof only.
 */
export default function ProposalNoLogoCoverReviewPage() {
  return (
    <div className="min-h-screen bg-slate-100 text-slate-900 antialiased">
      <ProposalNoLogoCoverReviewHarness />
    </div>
  );
}
